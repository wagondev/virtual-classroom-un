<%= form_with(model: team_inscription, local: true) do |form| %>
  <% if team_inscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(team_inscription.errors.count, "error") %> prohibited this team_inscription from being saved:</h2>

      <ul>
      <% team_inscription.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :rol %>
    <%= form.text_field :rol, id: :team_inscription_rol %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
